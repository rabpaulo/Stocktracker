services:
  stocktracker:
    build:
      context: .
    image: stocktracker:local
    user: "${STOCKTRACKER_UID:-1000}:${STOCKTRACKER_GID:-1000}"
    environment:
      HOME: /tmp
    volumes:
      - ./config:/app/config
    stdin_open: true
    tty: true
